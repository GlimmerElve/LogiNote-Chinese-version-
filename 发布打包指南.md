# LogiNote Pro 打包与发布指南

本文档说明：当你修改了代码、新增了功能后，如何打包并发布新版本，让用户通过「设置 → 软件更新 → 检查更新」自动收到更新。

---

## 一、一次性准备（只需做一次）

### 1. 确认 GitHub 仓库信息

自动更新走的是 GitHub Releases，仓库必须是 **public（公开）**，否则用户检查更新会失败。

| 项 | 值 | 备注 |
|----|----|----|
| 用户名（owner） | `GlimmerElve` | 注意末尾是 `Elve`（带 e），不是 `Elev` |
| 仓库名（repo） | `LogiNote-Chinese-version-` | 末尾有连字符 `-` |
| 仓库地址 | https://github.com/GlimmerElve/LogiNote-Chinese-version- | |

这两个值对应 `electron-builder.yml` 里的 `publish` 配置：

```yaml
publish:
  provider: github
  owner: GlimmerElve
  repo: LogiNote-Chinese-version-
```

### 2. 生成 GitHub Token（PAT）

发布（上传到 GitHub Releases）需要个人访问令牌：

1. GitHub 网页 → 右上角头像 → **Settings**
2. 左侧最底部 → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token (classic)**
4. 勾选 **`repo`** 权限（这是发布所需的最小权限）
5. 有效期建议选 **No expiration（永不过期）**，避免反复生成
6. 生成后**立即复制保存**（GitHub 只完整显示一次，刷新就看不到了）

> ⚠️ 安全：token 相当于你仓库的密码，务必妥善保存（密码管理器或本地文件），**不要贴进聊天、不要提交到 git 仓库**。

### 3. 国内网络加速（可选，但推荐）

国内访问 GitHub 下载 Electron 运行时（约 121MB）经常很慢或中断，建议发布时设置镜像源：

```powershell
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
```

---

## 二、每次发版的标准流程（三步）

### 第一步：修改版本号

打开 `package.json`，把 `version` 递增（遵循语义化版本，如 `1.0.0` → `1.0.1`）：

```json
{
  "version": "1.0.1"
}
```

### 第二步：打包并自动发布

在**项目根目录**打开 PowerShell，执行（把 `YOUR_TOKEN` 换成你的真实 token）：

```powershell
cd "C:\Users\Administrator\Desktop\AI编程测试\ios-风格智能学习计划笔记-(obsidian-loginote)"

# 设置发布 token（仅当前终端会话有效，不会写入文件）
$env:GH_TOKEN="YOUR_TOKEN"

# 国内镜像加速（可选但推荐）
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"

# 执行打包 + 自动发布
npm run publish
```

`npm run publish` 会自动执行：
1. `npm run build`（Vite 构建前端）
2. `npm run build:electron`（esbuild 打包 Electron 主进程）
3. `electron-builder --publish always`（打包 NSIS 安装包 + 上传到 GitHub Releases）

### 第三步：验证发布成功

1. 打开 GitHub Releases 页面：
   ```
   https://github.com/GlimmerElve/LogiNote-Chinese-version-/releases
   ```
2. 确认出现新版本（如 `V1.0.1`）。
3. 点进该 Release，确认有 **3 个资产**：
   - `LogiNote-Setup-1.0.1.exe`（安装包本体，约 534MB）
   - `LogiNote-Setup-1.0.1.exe.blockmap`（差分更新索引）
   - `latest.yml`（**关键！更新元数据**）

   如果这三个都在，就说明发布成功，用户端点「检查更新」即可收到新版本。

---

## 三、踩坑记录（务必注意）

### 坑 1：owner 拼写错误会报 404

之前 `owner` 写成了 `GlimmerElev`（少了末尾 `e`），导致 electron-builder 访问一个不存在的仓库地址，报错：
```
404 ... method: GET url: https://github.com/GlimmerElev/.../releases.atom
```
GitHub 对不存在的仓库统一返回 404，容易被误判为「token 错误」。正确值是 `GlimmerElve`（带 e）。

### 坑 2：文件名必须统一为连字符格式

`electron-builder.yml` 里配置了：
```yaml
artifactName: "${productName}-Setup-${version}.${ext}"
```
这保证生成的文件名为 `LogiNote-Setup-1.0.1.exe`（连字符），并且 `latest.yml` 里的 url 也是同样的名字。**不能**出现空格（`LogiNote Setup ...`）或点号（`LogiNote.Setup ...`），否则 electron-updater 找不到对应资产、更新失败。

### 坑 3：`latest.yml` 必须上传

electron-updater 检查更新的第一步就是下载 `latest.yml`。如果 Release 里只有 exe、没有 `latest.yml`，用户端会报：
```
Cannot find latest.yml in the latest release artifacts ... 404
```
自动发布偶尔会漏传 `latest.yml`，需要手动补传（见下面「手动兜底上传」）。

### 坑 4：token 泄露要立即撤销

如果不小心把 token 贴到了公开的地方（聊天、代码、截图），立即去 GitHub **撤销该 token**，重新生成一个新的。

---

## 四、手动兜底上传（自动发布漏传 latest.yml 时）

如果自动发布后，用户端仍报「找不到 latest.yml」，说明自动上传漏了它。手动补传步骤：

1. 打开 V1.x.x Release 的编辑页：
   ```
   https://github.com/GlimmerElve/LogiNote-Chinese-version-/releases/edit/V1.x.x
   ```
   （把 `V1.x.x` 换成实际版本 tag）

2. 删除旧的、文件名不一致的资产（如果有带点号或空格的旧文件）。

3. 把本地 `release` 目录下的 3 个文件拖到「Attach binaries」区域：
   ```
   release\LogiNote-Setup-1.x.x.exe
   release\LogiNote-Setup-1.x.x.exe.blockmap
   release\latest.yml
   ```

4. 点 **Update release** 保存。

5. 验证：访问
   ```
   https://github.com/GlimmerElve/LogiNote-Chinese-version-/releases/download/V1.x.x/latest.yml
   ```
   能打开看到 `version: 1.x.x` 即成功。

---

## 五、相关文件速查

| 文件 | 作用 |
|------|------|
| `package.json` | 版本号（`version`）、脚本 |
| `electron-builder.yml` | 打包与发布配置（owner / repo / artifactName / extraResources） |
| `release/` | 打包产物目录（exe / blockmap / latest.yml） |
| `src/electron/main.ts` | 自动更新 IPC（检查/下载/安装） |
| `src/components/SettingsModal.tsx` | 设置页「软件更新」按钮 |