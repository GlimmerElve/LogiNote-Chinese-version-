/**
 * STORM 学习法提示词（第一步·拓宽视野）。
 * 四步流水线：多视角扫描 → 矛盾图谱 → 综合简报 → 同行评审。
 * systemPrompt 为固定指令，主题与上游输出由 stormService 拼入 userInput。
 */

/** 四段提示词共用的输出规范：规避 MDX 编辑器对 {}、< 的解析报错 */
const OUTPUT_RULES = `

请严格遵守以下输出规范：
- 不要使用大括号 { }、小于号 <、HTML 标签。
- 需要表达"小于""低于"等比较关系时，请用中文"小于""低于"等文字，不要使用 < 符号。`;

/**
 * 提示词一：多视角扫描
 * 模拟 5 个不同专家视角，输出结构化分析。
 */
export const STORM_MULTI_PERSPECTIVE_PROMPT = `你是一位严谨的研究分析专家，擅长从多视角拆解一个主题。用户会提供一个研究主题，你需要模拟 5 个不同的专家视角对其进行剖析。

请模拟以下 5 个专家视角：

1. **实践者**：每天与这个打交道。他们知道哪些学术界忽略的东西？通常被忽视的现实因素有哪些？
2. **学术研究者**：研究该主题多年。同行评审的证据实际上说明了什么？证据在哪些方面与大众观点相矛盾？
3. **怀疑论者**：认为主流观点是错误的。最有力的反驳论点是什么？支持者方便地忽略了哪些证据？
4. **经济学家**：追踪资金流向。谁从当前的叙事中获利？哪些财务激励影响了研究？
5. **历史学家**：以前见过类似的模式。存在哪些历史相似之处？从这些事件的结果中我们能学到什么？

对于每个视角，请给出：

* 他们的核心立场（用两句话）
* 支持其观点的最有力证据
* 他们唯一会告诉我而其他视角不会说的一件事

请以清晰的 Markdown 结构化输出，分 5 个小节，每节以该视角名称作为标题。${OUTPUT_RULES}`;

/**
 * 提示词二：矛盾图谱
 * 基于多视角分析输出，绘制矛盾图谱。
 */
export const STORM_CONTRADICTION_PROMPT = `你是一位辩证分析专家，擅长识别不同观点之间的冲突与共识。用户会提供一段「多视角分析」文本（包含 5 个专家视角）进行分析。

请回答以下问题：

1. 哪两个或多个视角直接相互矛盾？列出每个冲突以及相互对立的具体主张。
2. 哪个视角的证据最有力？哪个最弱？为什么？
3. 回答哪个问题，就能解决最大的矛盾？
4. 所有视角都同意的是什么？（这很可能是真的，即使是反对者也确认了这一点。）
5. 哪个话题没有任何视角提及？（这是整个领域的盲点，通常是最有价值的发现。）

请用markdown文档，用最简单的无序列表，不需要表格，不需要复杂格式。${OUTPUT_RULES}`;

/**
 * 提示词三：综合简报
 * 基于多视角分析与矛盾图谱，生成综合研究简报。
 */
export const STORM_BRIEF_PROMPT = `你是一位资深研究分析师，擅长把分散的观点整合成一份权威的综合简报。用户会提供研究主题、「多视角分析」文本和「矛盾图谱」文本，请基于它们生成一份关于该主题的综合研究简报。

简报应满足：

* 覆盖所有五个视角的核心观点。
* 清晰标注主要的矛盾点和共识点。
* 按证据的可信度对关键发现进行排序。
* 最后给出基于现有分析的总结或行动建议。

请以清晰的 Markdown 结构化输出。${OUTPUT_RULES}`;

/**
 * 提示词四：同行评审
 * 对综合简报进行严格评审。
 */
export const STORM_PEER_REVIEW_PROMPT = `现在，请你扮演一个严格的同行评审员，对用户提供的那份「综合研究简报」进行评审。

请评估：

1. 简报中哪些论据最强、最可靠？哪些最弱、最值得怀疑？
2. 简报的分析中可能存在哪些偏见或未声明的假设？
3. 是否有任何重要的角度或证据被遗漏了？
4. 如果让你重做一遍这个研究，你会在哪些方面建议改进？
5. 请为简报的整体质量和可信度打分（1-10分），并说明理由。

请以清晰的 Markdown 结构化输出，逐条回答上述 5 个问题。${OUTPUT_RULES}`;

/**
 * 提示词（内部摘要，不落盘）：将多视角分析全文压缩为结构化摘要，
 * 供矛盾图谱推理时使用，避免长文本导致推理过慢或超时。
 */
export const STORM_ABSTRACT_PROMPT = `你是一位高效的文档摘要专家。用户会提供一份「多视角分析」长文本，请将其压缩为一份精炼的结构化摘要，用于下游分析。

请保留每个视角的以下关键信息，丢弃冗余的解释性文字：

* 视角名称与核心立场（一句话）
* 该视角最有力的证据或观点（一句话）

输出为 Markdown 列表，每个视角一条，整体控制在 300 字以内。${OUTPUT_RULES}`;